<!-- Animated terminal mockup + floating stat chips for the hero -->

<div class="hv-wrap">

  <!-- Floating stat chips -->
  <div class="hv-chip hv-chip--tl" aria-hidden="true">
    <span class="hv-chip-dot"></span>
    <span>73% faster delivery</span>
  </div>
  <div class="hv-chip hv-chip--br" aria-hidden="true">
    <span class="hv-chip-dot"></span>
    <span>$2.1M avg. client savings</span>
  </div>

  <!-- Glow behind terminal -->
  <div class="hv-glow" aria-hidden="true"></div>

  <!-- Terminal window -->
  <div class="hv-terminal" role="img" aria-label="Stack10 AI pipeline demo">
    <div class="hv-terminal-bar">
      <span class="hv-dot hv-dot--red"></span>
      <span class="hv-dot hv-dot--yellow"></span>
      <span class="hv-dot hv-dot--green"></span>
      <span class="hv-terminal-title">stack10 — ai pipeline</span>
    </div>
    <div class="hv-terminal-body">
      <div class="hv-line hv-line--1">
        <span class="hv-prompt">$</span>
        <span> stack10 analyze <span class="hv-accent">accounts-payable</span></span>
      </div>
      <div class="hv-line hv-line--2">
        <span class="hv-arrow">›</span>
        <span> Scanning 847 process variants<span class="hv-ellipsis">...</span></span>
      </div>
      <div class="hv-line hv-line--3">
        <span class="hv-check">✓</span>
        <span class="hv-success"> 12 high-value AI opportunities found</span>
      </div>
      <div class="hv-line hv-line--4">
        <span class="hv-check">✓</span>
        <span class="hv-success"> ROI projection: <strong>$2.4M / year</strong></span>
      </div>
      <div class="hv-line hv-line--5">
        <span class="hv-arrow">›</span>
        <span> Building production roadmap<span class="hv-ellipsis">...</span></span>
      </div>
      <div class="hv-line hv-line--6">
        <span class="hv-check">✓</span>
        <span class="hv-success"> Deployment ready — <strong>12 weeks</strong></span>
      </div>
      <div class="hv-line hv-line--7 hv-cursor-line">
        <span class="hv-prompt">$</span>
        <span class="hv-cursor"></span>
      </div>
    </div>
    <div class="hv-terminal-footer">
      <div class="hv-progress-track">
        <div class="hv-progress-fill"></div>
      </div>
      <div class="hv-progress-labels">
        <span>Pilot</span>
        <span class="hv-accent">Production</span>
      </div>
    </div>
  </div>

</div>

<style>
  .hv-wrap {
    position: relative;
    width: 100%;
    max-width: 520px;
    margin: 0 auto;
  }

  /* ── Glow ─────────────────────────────────────────────────── */
  .hv-glow {
    position: absolute;
    inset: -40px;
    background: radial-gradient(ellipse at 50% 50%, oklch(0.760 0.210 150 / 0.15) 0%, transparent 70%);
    pointer-events: none;
    z-index: 0;
  }

  /* ── Terminal shell ───────────────────────────────────────── */
  .hv-terminal {
    position: relative;
    z-index: 1;
    background: var(--gray-950);
    border: 1px solid oklch(0.35 0.02 150);
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow:
      0 0 0 1px oklch(0.760 0.210 150 / 0.08),
      0 24px 64px -12px rgb(0 0 0 / 0.5),
      0 8px 24px -4px rgb(0 0 0 / 0.3);
  }

  /* ── Title bar ────────────────────────────────────────────── */
  .hv-terminal-bar {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-4);
    background: oklch(0.18 0.008 150);
    border-bottom: 1px solid oklch(0.28 0.01 150);
  }

  .hv-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    flex-shrink: 0;
  }
  .hv-dot--red    { background: #ff5f57; }
  .hv-dot--yellow { background: #febc2e; }
  .hv-dot--green  { background: #28c840; }

  .hv-terminal-title {
    margin-left: var(--space-2);
    font-family: var(--font-mono);
    font-size: 11px;
    color: oklch(0.55 0.01 150);
    letter-spacing: 0.05em;
  }

  /* ── Terminal body ────────────────────────────────────────── */
  .hv-terminal-body {
    padding: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    font-family: var(--font-mono);
    font-size: 13px;
    line-height: 1.5;
  }

  .hv-line {
    display: flex;
    gap: var(--space-2);
    opacity: 0;
    transform: translateY(4px);
    animation: line-in 0.3s ease forwards;
  }

  .hv-line--1 { animation-delay: 0.4s; }
  .hv-line--2 { animation-delay: 1.0s; }
  .hv-line--3 { animation-delay: 1.9s; }
  .hv-line--4 { animation-delay: 2.5s; }
  .hv-line--5 { animation-delay: 3.2s; }
  .hv-line--6 { animation-delay: 4.1s; }
  .hv-line--7 { animation-delay: 4.8s; }

  @keyframes line-in {
    to { opacity: 1; transform: translateY(0); }
  }

  .hv-prompt { color: oklch(0.55 0.01 150); }
  .hv-arrow  { color: oklch(0.55 0.01 150); }
  .hv-check  { color: var(--green-400); }
  .hv-accent { color: var(--green-400); }
  .hv-success { color: oklch(0.85 0 0); }
  .hv-success strong { color: var(--green-400); font-weight: var(--weight-700); }

  /* Default line text colour */
  .hv-line span:not([class]) { color: oklch(0.72 0 0); }
  .hv-line > span:last-child { color: oklch(0.72 0 0); }

  /* Ellipsis pulse */
  .hv-ellipsis {
    animation: ellipsis-pulse 1.2s ease-in-out infinite;
    color: oklch(0.50 0 0);
  }
  @keyframes ellipsis-pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }

  /* Cursor blink */
  .hv-cursor {
    display: inline-block;
    width: 8px;
    height: 14px;
    background: var(--green-400);
    border-radius: 1px;
    vertical-align: middle;
    animation: cursor-blink 1s step-end infinite;
    opacity: 0;
    animation-delay: 4.9s;
  }
  @keyframes cursor-blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  /* ── Progress footer ──────────────────────────────────────── */
  .hv-terminal-footer {
    padding: var(--space-4) var(--space-6);
    border-top: 1px solid oklch(0.22 0.01 150);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .hv-progress-track {
    height: 4px;
    background: oklch(0.28 0.01 150);
    border-radius: var(--radius-full);
    overflow: hidden;
  }

  .hv-progress-fill {
    height: 100%;
    width: 0%;
    background: linear-gradient(90deg, var(--green-600), var(--green-400));
    border-radius: var(--radius-full);
    animation: progress-fill 2s ease forwards;
    animation-delay: 4.2s;
    box-shadow: 0 0 8px oklch(0.760 0.210 150 / 0.6);
  }
  @keyframes progress-fill {
    to { width: 88%; }
  }

  .hv-progress-labels {
    display: flex;
    justify-content: space-between;
    font-family: var(--font-mono);
    font-size: 10px;
    color: oklch(0.45 0 0);
  }

  /* ── Floating stat chips ──────────────────────────────────── */
  .hv-chip {
    position: absolute;
    z-index: 2;
    display: flex;
    align-items: center;
    gap: var(--space-2);
    background: var(--gray-0);
    border: 1px solid var(--border);
    border-radius: var(--radius-full);
    padding: var(--space-2) var(--space-4);
    font-size: 12px;
    font-weight: var(--weight-600);
    color: var(--gray-900);
    box-shadow: var(--shadow-md);
    white-space: nowrap;
    opacity: 0;
    animation: chip-in 0.5s ease forwards;
  }

  .hv-chip--tl {
    top: -18px;
    left: -24px;
    animation-delay: 2.2s;
    animation-name: chip-in-left;
  }
  .hv-chip--br {
    bottom: -18px;
    right: -24px;
    animation-delay: 4.4s;
    animation-name: chip-in-right;
  }

  @keyframes chip-in-left {
    from { opacity: 0; transform: translateX(-10px); }
    to   { opacity: 1; transform: translateX(0); }
  }
  @keyframes chip-in-right {
    from { opacity: 0; transform: translateX(10px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  /* Chip float animation after appearing */
  .hv-chip--tl { animation: chip-in-left 0.5s ease 2.2s forwards, chip-float 4s ease-in-out 2.7s infinite; }
  .hv-chip--br { animation: chip-in-right 0.5s ease 4.4s forwards, chip-float-rev 4s ease-in-out 4.9s infinite; }

  @keyframes chip-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }
  @keyframes chip-float-rev {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(6px); }
  }

  .hv-chip-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--green-500);
    flex-shrink: 0;
    box-shadow: 0 0 6px oklch(0.760 0.210 150 / 0.7);
    animation: dot-pulse 2s ease-in-out infinite;
  }
  @keyframes dot-pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
  }

  /* ── Responsive ───────────────────────────────────────────── */
  @media (max-width: 1023px) {
    .hv-chip { display: none; }
  }
</style>
